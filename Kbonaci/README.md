# Project description

A big int fibonnaci calculator using a custom big int implementation.
Also uses a third party big int library (num-bigint, rug, faer, astro-float) to compare results.
Used different implementations (explicit formula with golden ratio + big floats, iterative, recursive)
In Rust with lots of benchmarks (divan)
And made a plotter (simple-plot)

