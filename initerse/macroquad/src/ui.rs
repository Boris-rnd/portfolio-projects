use macroquad::prelude::*;


// pub fn draw_text_centered(t: &str, x: f32, y: f32, size: f32, color: Color) {
//     macroquad::prelude::draw_text(t, x-size*t.len() as f32/4.5, y, size, color)
// }

// pub fn draw_button(t: &str, x: f32, y: f32, size: f32, text_color: Color, w: f32, h: f32) {
//     draw_rectangle(x, y, w, h, color);
//     draw_text_centered(t, x, y, size, text_color);

// }
