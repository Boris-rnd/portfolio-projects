use bevy::prelude::*;

pub struct ExcaliMaterialPlugin;

impl Plugin for ExcaliMaterialPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: TASK-011
        // - Paper texture overlay material
        // - Dot grid material (shader)
        // - Wobbly shader with vertex displacement
    }
}
