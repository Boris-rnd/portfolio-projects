[package]
name = "su_core"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.15", features = ["default_font", "tonemapping_luts"] }
serde = { version = "1.0", features = ["derive"] }
